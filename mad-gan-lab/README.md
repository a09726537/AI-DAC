# MAD-GAN Lab

Minimal scaffold to experiment with Multivariate Anomaly Detection GAN on SQL / system logs.

## Quick start
```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python src/mad_gan_demo.py
```
